# Agent Workflow

Agents working in this repository must follow this branch and deployment
workflow.

1. Work only on the `work` branch.
2. For each meaningful unit of work, complete the full cycle of commit and push.
3. After pushing, open a pull request from `work` to `master` so the work can be
   merged into `master` and trigger the GitHub Pages GitHub Actions deployment.
